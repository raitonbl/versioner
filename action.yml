name: 'RaitonBL.com Versioner'
description: 'RaitonBL.com Versioner Github action'
branding:
  icon: box
  color: red
inputs:
  command:
    description: 'The command in the CLI application'
    required: true
  runtime:
    description: 'The runtime argument in the CLI application'
    required: true
  object:
    description: 'The object argument in the CLI application'
    required: true
  file:
    description: 'The file argument in the CLI application'
    required: true
  version:
    description: 'The CLI application version'
    required: false
    default: "2.1.0-RELEASE"
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.version }} ${{ inputs.command }} ${{ inputs.runtime }} ${{ inputs.object }} ${{ inputs.file }}